name: CI/CD
on: push
jobs:
  build-and-push-bundle:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    needs: [run-system-tests]
    if: github.ref == 'refs/heads/dev' || github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Cache Next.js 
        uses: actions/cache@v4
        id: next-cache-build
        with:
          path: |
            node_modules
          key: ${{ runner.os }}-portfolio-next-build-${{ github.run_id }}
          restore-keys: |
            ${{ runner.os }}-portfolio-next-build-
      - name: Install dependencies
        run: npm install
      - name: Build bundle
        run: npm run build
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/github-actions-rmodi
          aws-region: ${{ vars.AWS_REGION }}


      # - name: Prepare GitHub environment variables
      #   run: |
      #     set -eux
      #     # extract the repository name part
      #     repo="${GITHUB_REPOSITORY#*/}"
      #     # lowercase the name
      #     repo="${repo,,}"
      #     # replace / with _
      #     echo "ECR_REPOSITORY=${repo//\//_}" >> $GITHUB_ENV
      #     echo "COMMIT_HASH=${GITHUB_SHA}" >> $GITHUB_ENV
      # # TODO: put in actual values for these
      # - name: Login to Amazon ECR
      #   id: login-ecr
      #   uses: aws-actions/amazon-ecr-login@v1
      #   with:
      #     registries: ${{ secrets.AWS_ACCOUNT_ID }}
      #     mask-password: "true"
